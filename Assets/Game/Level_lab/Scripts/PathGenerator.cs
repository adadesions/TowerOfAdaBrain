using UnityEngine;

namespace Game.Level_lab
{
    public class PathGenerator : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
